## What

Brief description of the change.

## Why

What problem this solves or what it adds.

## Checklist

- [ ] `npm run build` passes (type-check clean)
- [ ] `npm test` passes (unit tests)
- [ ] `npm run test:e2e` passes (mock GeoLink e2e)
- [ ] `npm run smoke` passes (HTTP transport)
- [ ] README / tool descriptions updated if behavior or schema changed
- [ ] No secrets, API keys, or real coordinates from private data committed
- [ ] Follows the four extension points in `README.md#extending` if adding a field

## Related issues

Closes #
